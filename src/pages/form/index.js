import React from "react";
import { useFormik } from "formik";
import Page from "../../components/page";

export const Form = () => {
  return <Page content={"This is a form"} />;
};

export default Form;
